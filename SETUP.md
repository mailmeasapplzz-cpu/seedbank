# 미래를 심는 씨앗 — 웹사이트로 배포하기

이 사이트는 정적 파일(HTML/CSS/JS)이라 어디에 올려도 되지만, **여러 사람이 같은 대여 현황을 보려면** 데이터를 저장할 공용 데이터베이스가 필요해요. 무료인 Firebase(Firestore)를 붙여서 씁니다. 설치할 프로그램은 없고, 전부 웹 브라우저에서 클릭 + 복사·붙여넣기로 끝나요.

## 1단계. Firebase 프로젝트 만들기

1. https://console.firebase.google.com 접속 → 구글 계정으로 로그인
2. "프로젝트 추가" → 이름 입력(예: `future-seed-bank`) → 애널리틱스는 꺼도 됨 → 프로젝트 만들기

## 2단계. Firestore 데이터베이스 만들기

1. 왼쪽 메뉴 **빌드 → Firestore Database** → "데이터베이스 만들기"
2. 위치는 `asia-northeast3(서울)` 추천 → **테스트 모드로 시작** 선택 → 사용 설정

## 3단계. 보안 규칙 게시

1. Firestore 화면에서 **규칙(Rules)** 탭 클릭
2. 아래 내용을 전부 지우고, 이 프로젝트의 `firestore.rules` 파일 내용을 그대로 붙여넣기:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

3. **게시(Publish)** 클릭

> ⚠️ 이 규칙은 누구나 데이터를 읽고 쓸 수 있게 완전히 열어둔 상태예요. 로그인 비밀번호에 조건이 없는 것과 같은 맥락으로, 학교 프로젝트용으로 간단하게 만든 거예요. 실제 개인정보나 중요한 정보는 넣지 말고, 회원가입 비밀번호도 다른 곳에서 쓰는 진짜 비밀번호와 다르게 만들어 쓰라고 안내해주세요.

## 4단계. 웹 앱 연결 정보 복사 → 코드에 붙여넣기

1. Firebase 콘솔 왼쪽 위 **⚙️(프로젝트 설정)** → **일반** 탭 → 아래로 스크롤
2. "내 앱" 에서 **</> (웹)** 아이콘 클릭 → 앱 닉네임 입력(예: `web`) → 앱 등록
3. 화면에 나오는 `const firebaseConfig = { ... }` 코드를 통째로 복사
4. 이 프로젝트의 **`js/firebase-config.js`** 파일을 열어서, 안에 있는 `firebaseConfig` 객체를 방금 복사한 값으로 통째로 교체 (파일에 이미 형식이 적혀 있어요)
5. 저장

## 5단계. 사이트를 인터넷에 올리기 (둘 중 하나만 하면 돼요)

### 방법 A — GitHub Pages (추천, 계속 무료 유지)
1. https://github.com 에서 새 저장소(Repository) 만들기 (Public)
2. 저장소 페이지에서 **Add file → Upload files** → `seed-bank` 폴더 안의 파일/폴더 전체(`index.html`, `css`, `js` 등)를 그대로 드래그해서 업로드 → Commit
3. 저장소 **Settings → Pages** → Source를 `main` 브랜치로 설정 → Save
4. 잠시 후 `https://아이디.github.io/저장소이름` 주소로 접속 가능

### 방법 B — Netlify Drop (더 빠름, 계정 없이도 가능)
1. https://app.netlify.com/drop 접속
2. `seed-bank` 폴더를 통째로 화면에 드래그 앤 드롭
3. 몇 초 뒤 `https://무작위이름.netlify.app` 주소가 바로 생성됨 (계정을 만들면 주소 고정/재배포 가능)

## 6단계. 확인하기

1. 생성된 주소로 접속 → "불러오는 중이에요..." 후 로그인 화면이 뜨면 연결 성공
2. **관리자 / admin123** 으로 로그인해서 관리자 화면이 보이는지 확인
3. 다른 컴퓨터(또는 휴대폰)로 같은 주소에 접속해서 회원가입 → 씨앗 대여 신청
4. 관리자 계정에서 승인하면, 학생 화면이 새로고침 없이 자동으로 바뀌는지 확인

## 문제가 생기면

- 로그인 화면 대신 **"설정이 필요해요"** 가 보임 → `js/firebase-config.js` 값이 비어있거나 그대로임
- **"연결에 실패했어요"** 가 보임 → 3단계 보안 규칙 게시를 빠뜨렸거나, 4단계 설정 값 오타 확인 (브라우저 개발자 도구 콘솔에서 자세한 에러 확인 가능)
- 관리자 로그인이 안 됨 → 사이트에 처음 접속한 사람이 있어야 관리자 계정이 자동 생성돼요. 아직 아무도 접속 안 했다면 본인이 먼저 한 번 접속해보세요.
